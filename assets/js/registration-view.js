document.addEventListener('DOMContentLoaded', () => {
    setTimeout (() => {
        $entity.opportunity.evaluationMethodConfiguration._id = null;
        $entity.opportunity.evaluationMethodConfiguration.name = 'Aguardando certificação via edital';
    },0)
})
